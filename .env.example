# Este projeto usa Cloudflare Workers: variáveis vão no wrangler.toml [vars]
# e segredos em `wrangler secret put ...` ou no arquivo local `.dev.vars`.
#
# Veja também: .dev.vars.example e wrangler.toml
#
# Checkout WhatsApp (npm run dev): copie para .env na raiz
# PUBLIC_WHATSAPP_NUMBER=5511999999999
